# Orchestrator environment (PC2)

# Path to council topology JSON (edit IPs/ports for your LAN)
COUNCIL_CONFIG_PATH=council_config.json

# If you open the frontend via LAN IP, easiest is to allow all origins in dev:
# CORS_ALLOW_ORIGINS=*
CORS_ALLOW_ORIGINS=http://localhost:5173,http://localhost:3000

# Timeouts (seconds)
LLM_REQUEST_TIMEOUT_S=180
TITLE_REQUEST_TIMEOUT_S=30

