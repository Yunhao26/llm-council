# Worker environment (run on each LLM machine)

# council or chairman
WORKER_ROLE=council

# Display name shown in UI (recommend include model + machine)
WORKER_NAME=Council-1 (PC2)

# Local Ollama settings
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1:8b

# Bind worker service to LAN
WORKER_HOST=0.0.0.0

# Port suggestions:
# - council: 8002
# - chairman: 8003
WORKER_PORT=8002

